# JodFlow — ระบบล็อกอินของเราเอง · สมัครเอง · ทดลอง 14 วัน · แอดมินระบบ

> เขียน 9 ก.ย. 2569 · ทำตามแบบ `Jodflow.order/docs/LOGIN-SYSTEM-REFERENCE.md`
> สถานะ: **SQL รันแล้ว · โค้ดเสร็จแล้ว รอ deploy**

---

## 0. ตอบคำถามก่อน: ใช้ Supabase Auth ไหม

**ไม่ได้ใช้แล้ว** — ระบบล็อกอินเป็น **ของเราเอง** ตาราง `app_users` + `app_sessions` + token ที่ฐานเก็บแค่แฮช
เหมือน Jodflow.order ทุกกติกา

Supabase ยังใช้อยู่ 2 อย่างที่ไม่เกี่ยวกับล็อกอิน: **ฐานข้อมูล Postgres** และ **Storage** (ไฟล์แนบ)

`auth.users` ไม่ถูกลบ ไม่ถูกแก้ — แค่เลิกใช้ บัญชีทั้งหมดถูก**คัดลอก**มาที่ `app_users`
ด้วย `id` เดิมและแฮชรหัสผ่านเดิม (Supabase เก็บ bcrypt `$2a$10$…` ซึ่ง `crypt()` อ่านออก)
⇒ ทุกคนล็อกอินด้วย **รหัสผ่านเดิม** ได้ทันที และข้อมูลที่ชี้ถึง id เหล่านั้นไม่ขยับสักแถว

---

## 1. ข้อจำกัดที่ต่างจาก ref และทางแก้

| | Jodflow.order | Jodflow.money |
|---|---|---|
| ตัวแอป | Next.js — **มีเซิร์ฟเวอร์** | Vite SPA — **ไม่มีเซิร์ฟเวอร์เลย** |
| ชั้น 1 (ตรวจถูก ๆ) | `proxy.ts` ตรวจลายเซ็น HMAC บน edge | ตรวจว่า "มีตั๋วในเครื่องไหม" ในเบราว์เซอร์ |
| ชั้น 2 (ความจริง) | `getCurrentUser()` หา `AppSession` | RPC `app_me()` หา `app_sessions` |
| ที่เก็บตั๋ว | cookie `httpOnly` | `localStorage` + ส่งเองใน header `x-session-token` |
| ลายเซ็น HMAC | มี (`AUTH_SECRET` บนเซิร์ฟเวอร์) | **ไม่มี** — ไม่มีที่เก็บความลับ ทุกอย่างในแอปเป็นสาธารณะ |

**ทำไมไม่มีลายเซ็นแล้วยังปลอดภัย**: ลายเซ็นใน ref มีไว้ให้ชั้น 1 ปฏิเสธคนไม่มีตั๋ว
โดยไม่ต้องแตะฐาน (ประหยัด) ไม่ใช่ตัวตัดสินสิทธิ์ — ตัวตัดสินจริงคือชั้น 2 ทั้งสองระบบ
ที่นี่ทุก request ผ่าน PostgREST ซึ่งแตะฐานอยู่แล้ว การตรวจตั๋วจึงไม่มีต้นทุนเพิ่ม

**ราคาที่จ่ายจริง**: `localStorage` อ่านได้ด้วย JavaScript ในหน้า ⇒ ถ้ามี XSS ตั๋วหลุด
เป็นเหตุผลที่แอปนี้ต้องไม่โหลดสคริปต์ภายนอกใด ๆ (ตอนนี้โหลดแค่ Google Fonts) และห้าม `dangerouslySetInnerHTML`

---

## 2. หัวใจ: ทำไมไม่ต้องแก้ policy ทั้ง 26 ตาราง

policy ของทุกตาราง และ RPC การเงิน 13 ตัว (`post_transaction`, `pay_debt_entry`,
`close_card_statement`, …) ล้วนเรียก `auth.uid()` การไล่แก้ทุกตัวคือความเสี่ยงสูงสุดต่อข้อมูลจริง

ทางที่เลือก: **ให้ `auth.uid()` ตอบเป็นผู้ใช้ของเรา**

```
เบราว์เซอร์  ── header: x-session-token: <token 32 ไบต์> ──▶  PostgREST
                                                              │
                          pgrst.db_pre_request = app_pre_request()
                                                              │
                     อ่าน header → หา app_sessions (sha256) → ยังไม่หมดอายุ?
                     ไม่ถูกถอน? บัญชียัง is_active?
                                                              │
                     set_config('request.jwt.claims', {sub: user_id, ...})
                                                              ▼
                     ตั้งแต่บรรทัดนี้ auth.uid() = ผู้ใช้ของเรา
                     policy เดิม · RPC เดิม · Storage policy เดิม ทำงานต่อโดยไม่ต้องแก้
```

`app_pre_request()` ครอบด้วย `exception when others then return` ทั้งก้อน — มันรันก่อน**ทุก**
request ถ้าพังคือทั้งแอปล่ม ทุกทางที่ไม่เจอตั๋วให้เงียบแล้วปล่อยผ่านเป็น anon ตามปกติ

**ผลข้างเคียงที่ดี**: ถอนตั๋ว/ปิดบัญชี มีผลกับ request ถัดไปทันที ไม่ต้องรอ token หมดอายุ

---

## 3. เทียบทีละข้อกับไฟล์ ref

| ข้อใน ref | ที่นี่ |
|---|---|
| §2 ฐานเก็บแค่ sha256 ของ token ไม่เคยเก็บตัวจริง | ✅ `app_sessions.token_hash` |
| §2 อายุตั๋ว 30 วัน | ✅ |
| §4.1 ตอบ "อีเมลหรือรหัสผ่านไม่ถูกต้อง" เหมือนกันทุกกรณี | ✅ ไม่เจอบัญชี / บัญชีปิด / รหัสผิด = ข้อความเดียว |
| §4.1 ให้เวลาตอบใกล้กัน กันจับเวลาเดาว่าอีเมลไหนมีจริง | ✅ ไม่เจอบัญชีก็ยังคำนวณ bcrypt เท่าเดิม |
| §4.1 ล้างตัวนับก่อน redirect | ✅ (ทั้งหมดอยู่ในธุรกรรมเดียวของ RPC) |
| §4.2 โควตา 2 เครื่อง เตะเครื่องเก่าสุด | ✅ `app_issue_session` |
| §4.3 `getSessionUser` ผ่านครบ 4 ด่าน | ✅ `app_session_from_header()` |
| §4.4 `destroySession` / `destroyAllSessions` / กวาดของหมดอายุ | ✅ `app_logout` / `app_logout_all` / กวาดตอนออกตั๋ว |
| §4.5 กันลูป redirect ตอนตั๋วถูกถอน | ✅ ไม่มี redirect — `app_me()` คืน null → ทิ้งตั๋ว → หน้า login (ลูปเกิดไม่ได้) |
| §4.6 throttle 5/อีเมล · 20/IP · 15 นาที · sliding window | ✅ `app_throttle_check` (`login_attempts`) |
| §4.6 ลืมรหัสผ่านเข้มกว่า | ✅ 5 ครั้ง/ชั่วโมง |
| §4.6 อ่าน IP ไม่ได้ ⇒ ข้ามกติกา IP | ✅ `'unknown'` |
| §4.7 bcrypt cost 10 | ✅ (เท่ากับที่ Supabase ใช้ ⇒ แฮชเก่าใช้ต่อได้) |
| §4.7 ขั้นต่ำ 10 ตัว + กันรหัสยอดฮิต 12 คำ | ✅ `app_password_problem()` + `src/lib/passwordRules.js` |
| §4.8 ยืนยันด้วยวันเกิด + เบอร์ → ตั้งรหัสใหม่เอง | ✅ `app_reset_verify` / `app_reset_password` |
| §4.8 ไม่ผ่าน → เปิดคำขอให้แอดมินอัตโนมัติ | ✅ `password_reset_requests` |
| §4.8 ตั๋วรีเซ็ต 15 นาที ใช้ครั้งเดียว เก็บแฮช | ✅ `password_reset_tokens` |
| §4.8 ตั้งรหัสใหม่แล้วเตะทุกเครื่อง | ✅ |
| §4.8 บัญชีแอดมินใช้ทางนี้ไม่ได้ | ✅ |
| §4.10 สมัคร = user + shop + membership ธุรกรรมเดียว | ✅ `app_signup` |
| §7.6 ห้ามมี UI ตั้ง `isPlatformAdmin` | ✅ ตั้งได้จาก `users.sql` หมวด 13 ที่เดียว |
| §4.9 LINE / Google | ❌ ยังไม่ทำ (โครงรองรับ — เพิ่มคอลัมน์ `signup_channel` ไว้แล้ว) |
| §2 ลายเซ็น HMAC / cookie httpOnly | ❌ ทำไม่ได้ใน SPA (เหตุผลในหมวด 1) |

---

## 4. โครงตารางที่เพิ่ม

| ตาราง | หน้าที่ | ใครแตะได้ |
|---|---|---|
| `app_users` | บัญชี · `is_active` · `must_change_password` · `is_platform_admin` · `birth_date` · `phone` | **ไม่มีใคร** — RLS เปิดแต่ไม่มี policy + revoke ทุก role · เข้าถึงผ่าน RPC เท่านั้น |
| `app_sessions` | ตั๋วที่ยังมีชีวิต (`token_hash` · `expires_at` · `revoked_at`) | เหมือนกัน |
| `login_attempts` | ครั้งที่กรอกผิด แยกถัง login/reset/signup | เหมือนกัน |
| `password_reset_tokens` | ตั๋วตั้งรหัสใหม่ 15 นาที ใช้ครั้งเดียว | เหมือนกัน |
| `platform_settings` | วันทดลอง · เปิด-ปิดรับสมัคร · ข้อความหน้าสมัคร | ทุกคนอ่าน · แอดมินแก้ |
| `admin_audit_log` | แอดมินแตะร้านไหน เมื่อไหร่ ทำอะไร | แอดมินอ่าน/เขียน · **ไม่มี policy update/delete = แก้ย้อนหลังไม่ได้** |
| `password_reset_requests` | คิวคำขอให้แอดมิน | แอดมินเท่านั้น (สร้างโดย RPC — ไม่เปิดให้ใคร insert ตรง) |

คอลัมน์ที่เพิ่มบน `shops`: `status` (trial/active/suspended/closed) · `expires_at` · `trial_ends_at` · `suspend_reason` · `closed_at`

---

## 5. ช่องที่ปิดไว้

| ช่อง | ที่ปิด |
|---|---|
| ลูกค้าตั้งตัวเองเป็นแอดมิน | `app_users` แตะจากเบราว์เซอร์ไม่ได้เลย · ธงตั้งได้จาก SQL Editor ที่เดียว |
| ลูกค้าต่ออายุให้ตัวเอง | trigger `shops_guard_access` เขียน 5 คอลัมน์กลับค่าเดิมเสมอ ยกเว้น RPC แอดมิน (ตั้งธง `app.admin_write`) |
| ส่ง `expires_at` ปี 2099 ตอนสมัคร | `prepare_new_shop` เขียนทับด้วยค่าจาก `platform_settings` เสมอ |
| กดสมัครรัวสร้างร้านเปล่า | 1 บัญชี 1 ร้าน + throttle signup 5 ครั้ง/ชม./IP |
| แอดมินลืมเขียน audit log | ทุก RPC ที่เปลี่ยนแปลงอะไร เขียน log ในคำสั่งเดียวกัน |
| ไล่ตรวจว่าอีเมลไหนเป็นลูกค้าเรา | หน้าสมัคร/ล็อกอิน/ลืมรหัส ตอบข้อความกลาง ๆ เหมือนกันทุกกรณี |
| ยิงรัวเดาวันเกิดในหน้าลืมรหัส | throttle reset 5 ครั้ง/ชม. (วันเกิดมีแค่ ~36,000 แบบ ต้องเข้มกว่ารหัสผ่านมาก) |
| ฐานข้อมูลหลุด → สวมรอย | ไม่มีรหัสผ่านตัวจริง ไม่มี token ตัวจริง มีแต่แฮช |

---

## 6. สิ่งที่เปลี่ยนฝั่งหน้าจอ

### ไฟล์ใหม่
```
src/lib/sessionToken.js        เก็บ/อ่านตั๋ว + ตามแท็บอื่นเมื่อล็อกอิน-ออก
src/lib/api/auth.js            RPC ล็อกอินทั้งชุด (login/signup/me/logout/reset/sessions)
src/lib/passwordRules.js       กติการหัสผ่าน ยกจาก password.ts ของ ref
src/auth/AuthShell.jsx         โครงร่วมของหน้าก่อนเข้าแอป
src/auth/SignupPage.jsx        สมัคร (มีวันเกิด + เบอร์ สำหรับกู้รหัสเอง)
src/auth/ForgotPasswordPage.jsx  ยืนยันตัว → ตั้งรหัสใหม่ 2 ขั้น
src/auth/ChangePasswordPage.jsx  บังคับตั้งรหัสใหม่เมื่อเข้าด้วยรหัสชั่วคราว
src/auth/StatusBanners.jsx     แถบแดง "กำลังดูข้อมูลลูกค้า" + แถบเตือนวันหมดอายุ
src/lib/api/platform.js        เรื่องระดับแพลตฟอร์ม + จัดการบัญชีลูกค้า
src/pages/Admin/               หน้าแอดมิน 5 แท็บ
supabase/users.sql             ทุกอย่างฝั่งฐานข้อมูล
```

### ไฟล์ที่แก้
| ไฟล์ | แก้อะไร |
|---|---|
| `src/lib/supabase.js` | ครอบ `fetch` ให้แนบ `x-session-token` ทุก request · ปิด Supabase Auth ทั้งหมด |
| `src/auth/AuthProvider.jsx` | เขียนใหม่ทั้งไฟล์ — ตั๋วของเราเอง · สถานะ `must-change` / `blocked` · โหมดแอดมินดูร้านลูกค้า |
| `src/auth/AuthGate.jsx` | สลับหน้า login/สมัคร/ลืมรหัส/ตั้งรหัสใหม่ · หน้าหมดอายุ/ถูกระงับ/ปิดบัญชี |
| `src/auth/AccountPanel.jsx` | เปลี่ยนรหัส (ยืนยันรหัสเดิม) · ข้อมูลกู้บัญชี · รายการเครื่องที่ล็อกอินอยู่ + เตะทีละเครื่อง |
| `src/lib/realtime.js` | `postgres_changes` → `broadcast` (ตัวเดิมตรวจสิทธิ์ด้วย JWT ของ Supabase Auth ซึ่งไม่มีแล้ว) |
| `src/lib/api/context.js` | เพิ่ม `getUserId()` แทน `supabase.auth.getSession()` |
| `src/lib/api/logs.js` · `creditCards.js` | ใช้ `getUserId()` |
| `src/App.jsx` · `Sidebar.jsx` · `navConfig.js` · `router.jsx` · `Settings/index.jsx` | เส้นทาง `/admin` + เมนูเฉพาะแอดมิน + แถบสถานะ + การ์ดสถานะการใช้งาน |
| `index.html` | เพิ่มชื่อไอคอนใหม่ใน `icon_names` (ไม่เพิ่ม = ขึ้นเป็นตัวหนังสือแทนรูป) |

---

## 7. บทบาท 2 อย่าง

| | เจ้าของร้าน | แอดมินระบบ |
|---|---|---|
| เก็บที่ | `shop_members.role = 'owner'` | `app_users.is_platform_admin` |
| ตอนนี้คือใคร | `folkswagen.th@gmail.com` | `admin@admin.co` |
| เห็นอะไร | เฉพาะข้อมูลร้านตัวเอง | ลูกค้าทุกราย + เปิดดู/แก้ได้ทุกร้าน (มี audit log) |
| ตั้งยังไง | ลูกค้า: สมัครผ่านหน้าเว็บ · ร้านข้อมูลจริง: `users.sql` หมวด 13 | **`users.sql` หมวด 13 เท่านั้น** |

หมวด 13 ยังสร้าง **"พื้นที่ของแอดมินระบบ"** (ร้านเปล่า) ให้ `admin@admin.co` ด้วย —
เพราะแอปต้องผ่านด่าน "มีร้านอย่างน้อยหนึ่ง" ก่อนถึงจะเปิด `/admin` ได้

**บัญชี `folkswagen.th@gmail.com` ถ้ายังไม่มี ไฟล์สร้างให้พร้อมรหัสชั่วคราว** แสดงในตารางผลตรวจ
หัวข้อ 5 **ครั้งเดียว** — จดไว้ทันที เข้าครั้งแรกแล้วระบบบังคับตั้งรหัสใหม่

---

## 8. งานประจำหลังจากนี้

| งาน | ทำที่ไหน |
|---|---|
| ต่ออายุลูกค้า | `/admin/shops` → จัดการสิทธิ์ → ต่ออายุ |
| ระงับลูกค้าค้างชำระ | ที่เดียวกัน แท็บระงับ (บังคับกรอกเหตุผล ลูกค้าเห็นข้อความนั้น) |
| ลูกค้าลืมรหัสผ่าน | ลูกค้าทำเองได้ถ้ากรอกวันเกิด+เบอร์ไว้ · ไม่ได้ → เข้าคิว `/admin/resets` → **ยืนยันตัวด้วยคนก่อน** → `/admin/shops` → ออกรหัสชั่วคราว |
| ลูกค้าแจ้งเครื่องหาย | `/admin/shops` → ออกจากทุกเครื่อง |
| ปิดบัญชีลูกค้า | `/admin/shops` → ปิดบัญชี (ต่างจาก "ระงับร้าน": ปิดบัญชี = ล็อกอินไม่ได้เลย) |
| เข้าไปแก้ข้อมูลให้ลูกค้า | `/admin/shops` → เปิดข้อมูล → แถบแดงค้างบนจอ + ลง audit log |
| เปลี่ยนวันทดลอง / ปิดรับสมัคร | `/admin/settings` (มีผลทันที ไม่ต้อง deploy) |
| ตรวจสุขภาพฐานข้อมูล | รัน `supabase/check.sql` |

**ไม่ต้องเข้าแดชบอร์ด Supabase อีกเลย** สำหรับงานบัญชีผู้ใช้ — ทุกอย่างทำในแอปได้แล้ว

---

## 9. สิ่งที่ตั้งใจ **ไม่** ทำในรอบนี้

- ล็อกอินด้วย LINE / Google (ref §4.9) — ต้องมีเซิร์ฟเวอร์แลก `code` เป็น token เพราะ client secret ห้ามลงเบราว์เซอร์
- ส่งอีเมลอัตโนมัติ (ยืนยันอีเมล · ลิงก์รีเซ็ต · เตือนก่อนหมดอายุ) — ต้องต่อ SMTP
- เชิญคนอื่นเข้าร้านเดียวกัน (โครง `shop_members` + role editor/viewer รองรับแล้ว ยังไม่มี UI)
- เก็บเงินอัตโนมัติ — ต่ออายุด้วยมือจากหน้าแอดมิน
- แบ่งชั้นแอดมิน (เจ้าของ vs ผู้ช่วย)

---

## 10. เรื่องที่ต้องจัดการนอกโค้ด

**องค์กร Supabase อยู่แผน FREE และเลยช่วงผ่อนผันแล้ว** (แถบส้มบนแดชบอร์ด: *Grace period is over ·
Your projects will not be able to serve requests when you use up your quota*)

โควตาหมดเมื่อไหร่ = โปรเจกต์หยุดตอบ request = แอปล่มทั้งระบบ ทั้งของเราและของลูกค้า
ข้อมูลไม่หาย (โปรเจกต์แค่หยุดให้บริการ) แต่ก่อนเปิดรับลูกค้าที่จ่ายเงินจริง ควรอัปเป็น Pro ก่อน
